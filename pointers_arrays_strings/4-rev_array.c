#include "main.h"

/**
 * reverse_array - dar vuelta un arreglo
 * @a: arreglo a dar vuelta
 * @n: largo del arreglo
 *
 * Return: void, no retorna nada, solo invierte un array

void reverse_array(int *a, int n)
