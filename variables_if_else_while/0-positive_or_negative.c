#include<stdio.h>
/**
*main - algo
*Return: que signo tiene el numero
*/

int main(int n)
{
	if (n < 0)
	{
	printf("%d is negative", n);
	}
	 else
	{
	if (n = 0)
	{
	print("%d is zero", n);
	}
	else
	{
	printf("%d is positive", n);
	}

	return (0);
}
